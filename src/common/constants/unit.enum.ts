export enum PRICE_ENUM {
    USD="usd",
    CAD="cad"
}

export enum DISTANCE_ENUM {
    MILES='miles',
    KILOMETERS='km'

}

export enum WEIGHT_ENUM {
    POUND='pound',
    KILOGRAMS='kilograms'
}

export enum LENGTH_ENUM {
    FEET='feet',
    METER='meter'

}