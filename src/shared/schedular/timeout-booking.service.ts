//

import { Injectable } from "@nestjs/common";




@Injectable()
export class TimeoutBookingsSchedular {
    constructor(
    ) { }

    async execute(parameters: Record<string, any>) {
        try {
     
            
        } catch (error) {
            throw error
        }
    }
}